---
'@react-tela/core': patch
'react-tela': patch
---

Add viewport/scroll behavior to `<Group>` component with `contentWidth`, `contentHeight`, `scrollTop`, and `scrollLeft` props for scrollable content areas
