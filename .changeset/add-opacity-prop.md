---
'react-tela': minor
---

Add `opacity` prop to all entities as an alias for `alpha`. Maps to `ctx.globalAlpha` on CanvasRenderingContext2D. When both `alpha` and `opacity` are specified, `opacity` takes precedence.
